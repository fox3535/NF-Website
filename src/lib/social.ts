/**
 * NF's social links.
 *
 * Not documented anywhere in project data yet (docs/event-data.md has no
 * social handles). Do not invent one. Fill this in once the real Nostalgia
 * Fest Instagram URL is confirmed — until then, anything that would link
 * there renders without a working destination rather than pointing
 * somewhere fake.
 */
export const INSTAGRAM_URL: string | null = null;
